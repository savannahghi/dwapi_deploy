---

# MySQL root password
mysql_root_pwd: "{{ mysql_root_pwd }}"

# Dwapi db settings
db_host: "{{ db_host }}"
db_user: "{{ db_user }}"
db_name: "{{ db_name }}"
db_password: "{{ db_password }}"
db_port: "{{ db_port }}"

# Dwapi host settings
server_domain: "{{ server_domain }}"
server_user_password: "{{ server_user_password }}"
dwapi_port_one: "{{ dwapi_port_one }}"
dwapi_port_two: "{{ dwapi_port_two }}"